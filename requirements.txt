anthropic>=0.34.0
pydantic>=2.0
rapidfuzz>=3.0
pandas>=2.0
typer>=0.12
pytest>=8.0

# Optional (only needed if you wire in the "should-have"/"nice-to-have" integrations)
# razorpay>=1.4
# google-generativeai>=0.8.0
